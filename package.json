{
  "name": "notizen-api",
  "version": "1.0.0",
  "description": "API for Notizen summarization service",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
